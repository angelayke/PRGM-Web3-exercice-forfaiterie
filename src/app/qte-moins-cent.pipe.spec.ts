import { QteMoinsCentPipe } from './qte-moins-cent.pipe';

describe('QteMoinsCentPipe', () => {
  it('create an instance', () => {
    const pipe = new QteMoinsCentPipe();
    expect(pipe).toBeTruthy();
  });
});
